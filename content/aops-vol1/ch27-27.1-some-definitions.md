---
chapter: 27
chapter_title: "Sets"
section: "27.1"
section_title: "Some Definitions"
book_pages: [246]
pdf_pages: [260]
---

# 27.1 Some Definitions

<!-- pdf page 260 (book p246) -->
![Source page 246](raster/page-260.jpg)

Chapter 27

Sets 27.1

Some Definitions

A set is a collection of objects. For example, the set of all past Presidents of the United States, the set of all real numbers, and the set of all copies of this textbook are all valid sets. A set is usually denoted by placing the objects in the set inside a pair of curly braces; thus the set of even positive integers less than 10 is given by 12,4,6,8}. Some sets are too long to specify in this way: for example, the set of Presidents, {Washington, Adams, Jefferson, Monroe, Madison,...} . In such a case, we can write the set like this: {x |x is a former President}. Here x is a dummy variable, which simply takes on all values specified by the condition after the vertical bar. Another example could be the set of all points in the Cartesian plane: { [x, y) |x and y are real numbers}. Here there are two dummy variables, x and y; the values that each can take must be specified. Sets can have names, to make writing them simpler. For example, we could define P to be the set of former Presidents; when we wish to refer to the set, we can just write P rather than { x |x is a former President}. Each of the major types of numbers (see Chapter 8) has a special letter-like symbol which always refers to that set. The set of real numbers is denoted by R, using a stylized R for "reals." Similarly, the set of complex numbers is written C. Unfortunately, the symbol for the integers, Z, was invented in Germany, so is based on a Z rather than an I. The rationals are denoted by Q, where the Q stands for "quotients." Remember these; their use will become more and more common as you get into more advanced problems. Instead of saying "Find all real numbers x. .. a problem might say "Find all x e R". Be ready. An object in a set is called an element of the set. Thus Grover Cleveland is an element of { x |x is a former President), and n is an element of { x \x is a real number}. We sometimes use the symbol e to denote being an element, so that we could rewrite one statement above as "Grover Cleveland e P." A cross through the e just means not, so that Aaron Burr i P. Since a set is just a collection, it doesn't make any sense for one element to be in twice; each element in a set can occur
